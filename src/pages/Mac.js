import React from "react";

// 단축키 rsf
function Mac() {
  //           유저            react library
  //jsx : Javascript XML -> HTML변환 -> 화면배치
  // root element는 단 1개만 존재!!
  return (
    <div>
      <p>Mac화면입니다.</p>
    </div>
  );
}

export default Mac;
